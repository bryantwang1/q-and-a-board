
import { totalVotes } from 'q-and-a-board/helpers/total-votes';
import { module, test } from 'qunit';

module('Unit | Helper | total votes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalVotes([42]);
  assert.ok(result);
});

